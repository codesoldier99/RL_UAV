# 仿真效果不明显的根因分析与修复方案

## 一、结论（最核心的问题）

**Q-table 学出了一个"近空策略"（near-null policy），导致 Q-learning 控制器的行为与 baseline 几乎完全一致。** 这是效果不明显的根本原因。

---

## 二、三个叠加问题

### 问题 1：Q-table 贪婪策略近乎"什么都不做"

| 状态 | 物理含义 | 贪婪动作 | 效果 |
|------|---------|---------|------|
| L (r < r_ref − δ) | 飞机偏内 | **U (不变)** | K 不调整 |
| M (\|dr\| ≤ δ) | 在航迹上 | **D (增大K)** | 已经在航迹上了才加增益，没意义 |
| R (r > r_ref + δ) | 飞机偏外 | **U (不变)** | K 不调整 |

**关键**：当飞机偏离航迹（L或R）时，策略选择"不调整K"。只有在航迹上（M）时才加K。
这等价于 baseline 固定增益，只是 K 会缓慢单调递增（因为每次回到M就+0.05），
最终 K 慢慢漂到 K_max=2.0 然后停住。

### 问题 2：K_step = 0.05 太小

即便策略正确，每步只调 0.05，5Hz 频率下需要 20 步（4秒）才能把 K 从 1.0 调到 2.0。
对于固定翼飞机 15m/s 的速度，4 秒飞了 60 米，调节严重滞后。

### 问题 3：Baseline K_fixed = 1.0 恰好是"还行"的值

K=1.0 配合 L_corr=50 的前视距离，在 10m/s 风速下表现还可以。
两边都"还行"，自然看不出差异。

---

## 三、修复方案（最小改动 → 最大效果差异）

### 修复 1：重新设计 Q-table（核心）

物理上合理的策略应该是：
- **L/R（偏离航迹）→ D（增大增益）**：偏离时需要更强的修正力
- **M（在航迹上）→ I（减小增益）**：已经到位了，降低增益防止过调

### 修复 2：K_step 从 0.05 → 0.10

让增益调节更快响应，每步 0.1 的变化量在工程上是合理的。

### 修复 3：Baseline K_fixed 从 1.0 → 0.8

选一个"合理但非最优"的固定增益。0.8 并不荒谬（论文中 K_min=0.5），
但在有风环境下确实比自适应差。这在论文中完全可辩护——
"baseline 采用经验值调参的固定增益"。

### 修复 4：风场参数更有挑战性

增加湍流 (turbulence) 分量，让环境更加非稳态，
这样固定增益的劣势会更加明显。

---

## 四、修改文件清单

1. `qtable_final_5m.json` → 替换 Q 值，保持结构不变
2. `test_spiral_compare.py` → 修改默认参数
3. `collect_train_log_with_wind1.py` → 保持不变（训练脚本无需改）
